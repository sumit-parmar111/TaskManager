import { CountryuniquevalidatorDirective } from './countryuniquevalidator.directive';

describe('CountryuniquevalidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CountryuniquevalidatorDirective();
    expect(directive).toBeTruthy();
  });
});
